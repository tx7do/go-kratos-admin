// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.8.3
//   protoc               unknown
// source: pagination/v1/pagination.proto

/* eslint-disable */
import type { Any } from "../../google/protobuf/any.pb";

/** 操作符枚举 */
export enum Operator {
  /** OPERATOR_UNSPECIFIED - 未指定 */
  OPERATOR_UNSPECIFIED = "OPERATOR_UNSPECIFIED",
  /** EQ - 基本比较 */
  EQ = "EQ",
  /** NEQ - != */
  NEQ = "NEQ",
  /** GT - > */
  GT = "GT",
  /** GTE - >= */
  GTE = "GTE",
  /** LT - < */
  LT = "LT",
  /** LTE - <= */
  LTE = "LTE",
  /** LIKE - 模糊 / 大小写不敏感模糊 / 非模糊 */
  LIKE = "LIKE",
  /** ILIKE - 大小写不敏感的 LIKE（如 Postgres ILIKE） */
  ILIKE = "ILIKE",
  /** NOT_LIKE - NOT LIKE */
  NOT_LIKE = "NOT_LIKE",
  /** IN - 集合操作 */
  IN = "IN",
  /** NIN - NOT IN */
  NIN = "NIN",
  /** IS_NULL - 空值判断 */
  IS_NULL = "IS_NULL",
  /** IS_NOT_NULL - IS NOT NULL */
  IS_NOT_NULL = "IS_NOT_NULL",
  /** BETWEEN - 范围与正则 */
  BETWEEN = "BETWEEN",
  /** REGEXP - 正则匹配（DB/引擎支持） */
  REGEXP = "REGEXP",
  /** IREGEXP - 不区分大小写的正则匹配 */
  IREGEXP = "IREGEXP",
  /** CONTAINS - 语义化的字符串操作 */
  CONTAINS = "CONTAINS",
  /** STARTS_WITH - 前缀（等价于 LIKE v%） */
  STARTS_WITH = "STARTS_WITH",
  /** ENDS_WITH - 后缀（等价于 LIKE %v） */
  ENDS_WITH = "ENDS_WITH",
  /** ICONTAINS - 不区分大小写的包含 */
  ICONTAINS = "ICONTAINS",
  /** ISTARTS_WITH - 不区分大小写的前缀匹配 */
  ISTARTS_WITH = "ISTARTS_WITH",
  /** IENDS_WITH - 不区分大小写的后缀匹配 */
  IENDS_WITH = "IENDS_WITH",
  /** JSON_CONTAINS - JSON / 数组 / 集合相关（按需在服务端映射为具体 DB 运算） */
  JSON_CONTAINS = "JSON_CONTAINS",
  /** ARRAY_CONTAINS - 数组包含某元素 */
  ARRAY_CONTAINS = "ARRAY_CONTAINS",
  /** EXISTS - 子查询 / 存在性（按需实现） */
  EXISTS = "EXISTS",
  /** SEARCH - 全文/模糊搜索（服务端按需实现） */
  SEARCH = "SEARCH",
  /** EXACT - 精确匹配（完全相等） */
  EXACT = "EXACT",
  /** IEXACT - 不区分大小写的精确匹配 */
  IEXACT = "IEXACT",
}

/** 日期时间部分枚举 */
export enum DatePart {
  DATE_PART_UNSPECIFIED = "DATE_PART_UNSPECIFIED",
  /** DATE - 日期 */
  DATE = "DATE",
  /** YEAR - 年 */
  YEAR = "YEAR",
  /** ISO_YEAR - ISO 8601 一年中的周数 */
  ISO_YEAR = "ISO_YEAR",
  /** QUARTER - 季度 */
  QUARTER = "QUARTER",
  /** MONTH - 月 */
  MONTH = "MONTH",
  /** WEEK - ISO 8601 周编号 一年中的周数 */
  WEEK = "WEEK",
  /** WEEK_DAY - 星期几 */
  WEEK_DAY = "WEEK_DAY",
  /** ISO_WEEK_DAY - ISO 8601 星期几 */
  ISO_WEEK_DAY = "ISO_WEEK_DAY",
  /** DAY - 日 */
  DAY = "DAY",
  /** TIME - 时间（小时:分钟:秒） */
  TIME = "TIME",
  /** HOUR - 小时 */
  HOUR = "HOUR",
  /** MINUTE - 分钟 */
  MINUTE = "MINUTE",
  /** SECOND - 秒 */
  SECOND = "SECOND",
  /** MICROSECOND - 微秒 */
  MICROSECOND = "MICROSECOND",
}

/** 过滤表达式类型 */
export enum ExprType {
  EXPR_TYPE_UNSPECIFIED = "EXPR_TYPE_UNSPECIFIED",
  AND = "AND",
  OR = "OR",
}

/** 排序规则（分页场景通常需配合排序保证结果稳定） */
export interface Sorting {
  /** 排序字段（如"id"、"create_time"） */
  field: string;
  order: Sorting_Order;
}

/** 排序方向（ASC/DESC，默认ASC） */
export enum Sorting_Order {
  ASC = "ASC",
  DESC = "DESC",
}

/** 单个条件 */
export interface Condition {
  /** 过滤字段名 */
  field: string;
  /** 过滤操作符 */
  op: Operator;
  /** 过滤值（单值） */
  value?:
    | string
    | null
    | undefined;
  /** 过滤值（多值，如IN操作符） */
  values: string[];
}

/** 过滤表达式 */
export interface FilterExpr {
  /** 过滤表达式类型 */
  type: ExprType;
  /** 条件列表 */
  conditions: Condition[];
  /** 子表达式列表 */
  groups: FilterExpr[];
}

/** 页码-页大小分页（Page-PageSize） */
export interface PageBasedPagination {
  /** 当前页码（从1开始，默认1） */
  page: number;
  /** 每页条数（默认10，建议设置上限如100） */
  pageSize: number;
}

/** 偏移量-限制分页（Offset-Limit） */
export interface OffsetBasedPagination {
  /** 跳过的记录数（从0开始，默认0） */
  offset: string;
  /** 最多返回的记录数（默认10，建议设置上限如100） */
  limit: number;
}

/** 令牌分页（Token-Based） */
export interface TokenBasedPagination {
  /** 上一页最后一条记录的游标（如ID/时间戳+ID，首次请求为空） */
  token: string;
  /** 每页条数（默认10，建议设置上限如100） */
  pageSize: number;
}

/** 不分页 */
export interface NoPaging {
}

/**
 * ------------------------------
 * 分页通用请求
 * ------------------------------
 */
export interface PagingRequest {
  /** 当前页码（从1开始，默认1） */
  page?:
    | number
    | null
    | undefined;
  /** 每页条数（默认10，建议设置上限如100） */
  pageSize?:
    | number
    | null
    | undefined;
  /** 跳过的记录数（从0开始，默认0） */
  offset?:
    | string
    | null
    | undefined;
  /** 最多返回的记录数（默认10，建议设置上限如100） */
  limit?:
    | number
    | null
    | undefined;
  /** 上一页最后一条记录的游标（如ID/时间戳+ID，首次请求为空） */
  token?:
    | string
    | null
    | undefined;
  /** 是否不分页，如果为true，则page和pageSize参数无效。 */
  noPaging?:
    | boolean
    | null
    | undefined;
  /** 排序条件，其语法为JSON字符串，例如：{"val1", "-val2"}。字段名前加'-'为降序，否则为升序。 */
  orderBy: string[];
  /** 排序规则（可选，建议必传以保证分页结果稳定） */
  sorting: Sorting[];
  /** AND过滤参数，其语法为json格式的字符串，如：{"key1":"val1","key2":"val2"}，具体请参见：https://github.com/tx7do/go-utils/tree/main/entgo/query/README.md */
  query?:
    | string
    | null
    | undefined;
  /** OR过滤参数，语法同AND过滤参数。 */
  or?:
    | string
    | null
    | undefined;
  /** 复杂过滤表达式 */
  filterExpr?:
    | FilterExpr
    | null
    | undefined;
  /** 字段掩码，其作用为SELECT中的字段，其语法为使用逗号分隔字段名，例如：id,realName,userName。如果为空则选中所有字段，即SELECT *。 */
  fieldMask?: string[] | null;
}

/**
 * ------------------------------
 * 分页响应元数据
 * ------------------------------
 */
export interface PaginationResponseMeta {
  /** 总记录数（仅Page/Offset分页有效，Token分页通常不返回总数） */
  total?:
    | string
    | null;
  /** 总页数（仅Page分页有效） */
  totalPages?:
    | number
    | null;
  /** 当前页码（仅Page分页有效） */
  currentPage?:
    | number
    | null;
  /** 当前偏移量（仅Offset分页有效） */
  currentOffset?:
    | string
    | null;
  /** 下一页令牌（仅Token分页有效，无更多数据时为空） */
  next_token?:
    | string
    | null
    | undefined;
  /** 每页实际条数 */
  pageSize?:
    | number
    | null
    | undefined;
  /** 当前页实际返回条数 */
  currentSize?: number | null | undefined;
}

/**
 * ------------------------------
 * 分页通用结果
 * ------------------------------
 */
export interface PagingResponse {
  /** 总记录数 */
  total?:
    | string
    | null;
  /** 分页数据 */
  items: Uint8Array[];
}

/**
 * ------------------------------
 * 通用分页请求
 * ------------------------------
 */
export interface PaginationRequest {
  /** 分页类型（三种方式互斥，必须选其一） */
  paginationType?:
    | //
    /** 基于页码的分页方式 */
    { $case: "pageBased"; pageBased: PageBasedPagination }
    | //
    /** 基于偏移量的分页方式 */
    { $case: "offsetBased"; offsetBased: OffsetBasedPagination }
    | //
    /** 基于令牌的分页方式 */
    { $case: "tokenBased"; tokenBased: TokenBasedPagination }
    | //
    /** 不分页 */
    { $case: "noPaging"; noPaging: NoPaging }
    | null;
  /**
   * 排序条件，其语法为JSON字符串，例如：{"val1", "-val2"}。字段名前加'-'为降序，否则为升序。
   *
   * @deprecated
   */
  orderBy: string[];
  /** 排序规则（可选，建议必传以保证分页结果稳定） */
  sorting: Sorting[];
  /**
   * AND过滤参数，其语法为json格式的字符串，如：{"key1":"val1","key2":"val2"}，具体请参见：https://github.com/tx7do/go-utils/tree/main/entgo/query/README.md
   *
   * @deprecated
   */
  query?:
    | string
    | null
    | undefined;
  /**
   * OR过滤参数，语法同AND过滤参数。
   *
   * @deprecated
   */
  or?:
    | string
    | null
    | undefined;
  /** 复杂过滤表达式（优先使用） */
  filterExpr?:
    | FilterExpr
    | null
    | undefined;
  /** 字段掩码，其作用为SELECT中的字段，其语法为使用逗号分隔字段名，例如：id,realName,userName。如果为空则选中所有字段，即SELECT *。 */
  fieldMask?: string[] | null;
}

/**
 * ------------------------------
 * 通用分页响应
 * ------------------------------
 */
export interface PaginationResponse {
  /** 分页元数据 */
  meta:
    | PaginationResponseMeta
    | null;
  /** 业务数据列表（示例用Any，实际业务需替换为具体message，如repeated User users = 1） */
  data: Any[];
}
